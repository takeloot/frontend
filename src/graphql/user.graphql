fragment RegularProfile on Profile {
  id
  provider
  serviceId
}

query me {
  me {
    id
    name
    avatar
    profiles {
      ...RegularProfile
    }
  }
}

mutation logout {
  logout
}

mutation updateConnectionStatus {
  updateConnectionStatus
}
