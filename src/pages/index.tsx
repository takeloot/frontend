import type {NextPage} from "next";

import {MainLayout} from "_app/layouts/main-layout";

const Home: NextPage = () => {
  return (
    <MainLayout>
      <div>Landing</div>
    </MainLayout>
  );
};

export default Home;
